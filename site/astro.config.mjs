import { defineConfig } from "astro/config";

// Project-pages URL: https://JacksonGreenNZ.github.io/TheSepulchre
// When you attach a custom domain, set base: "/" and site to your domain.
export default defineConfig({
  site: "https://thesepulchre.org",
  base: "/",
});
