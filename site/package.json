{
  "name": "the-sepulchre-site",
  "type": "module",
  "private": true,
  "scripts": {
    "collect": "node ../scripts/collect.mjs",
    "dev": "npm run collect && astro dev",
    "build": "npm run collect && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.15.0",
    "js-yaml": "^4.1.0",
    "marked": "^12.0.0"
  }
}
